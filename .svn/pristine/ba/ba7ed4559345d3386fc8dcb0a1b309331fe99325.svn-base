import React from 'react';
import colors from '../configs/Colors';
import {
    TouchableOpacity,
} from 'react-native';
import {
    Icon,
} from 'react-native-elements';
export default({navigation}) =>
    <TouchableOpacity
        onPress={() => navigation.goBack()}>
        <Icon name='ios-arrow-back' type='ionicon' size={32} color={colors.default}/>
    </TouchableOpacity>